name: Refresh scientist portfolio data

on:
  workflow_dispatch:
    inputs:
      scopus_author_id:
        description: "Optional Scopus Author ID override"
        required: false
        default: "57220956828"
        type: string
      mirror_craftum_assets:
        description: "Download/mirror Craftum assets into repository"
        required: false
        default: "true"
        type: choice
        options:
          - "true"
          - "false"
  schedule:
    - cron: "17 3 * * 1"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

permissions:
  contents: write
  issues: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y openvpn curl iproute2 xvfb
      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests beautifulsoup4 pyyaml rapidfuzz argostranslate playwright
      - name: Install browsers for browser-backed data fetchers
        run: |
          python -m playwright install --with-deps chromium
          python -m playwright install chrome || true
          google-chrome --version || true
      - name: Mirror Craftum assets
        run: |
          if [ "${{ inputs.mirror_craftum_assets || 'true' }}" = "true" ] && [ -f scripts/mirror_craftum_assets.py ]; then
            python scripts/mirror_craftum_assets.py || true
          else
            echo "Craftum asset mirroring skipped."
          fi
      - name: Harvest open bibliographic sources
        env:
          PROFILE_YAML: config/profile.yml
          OPENALEX_MAILTO: omnistat@yandex.ru
        run: |
          if [ -f scripts/harvest_open_sources.py ]; then
            python scripts/harvest_open_sources.py || true
          else
            echo "Open sources harvester is missing; skipping ORCID/OpenAlex/Crossref."
          fi
      - name: Harvest free media mentions
        env:
          PROFILE_YAML: config/profile.yml
        run: |
          if [ -f scripts/harvest_media_mentions.py ]; then
            python scripts/harvest_media_mentions.py || true
          else
            echo "Media mentions harvester is missing; skipping RSS media search."
          fi
      - name: Enhance media mention metadata and institutional scan
        env:
          PROFILE_YAML: config/profile.yml
          MEDIA_SOURCES_YAML: config/media_sources.yml
        run: |
          if [ -f scripts/enhance_media_mentions.py ]; then
            python scripts/enhance_media_mentions.py || true
          else
            echo "Media enhancer is missing; skipping metadata repair and institutional scan."
          fi
      - name: Normalize append-only media snapshot
        run: |
          if [ -f scripts/normalize_media_snapshot.py ]; then
            python scripts/normalize_media_snapshot.py --normalize-only || true
          else
            echo "Media snapshot normalizer is missing; skipping post-processing."
          fi
      - name: Connect home OpenVPN route for eLibrary and WoS
        env:
          ELIBRARY_OPENVPN_CONFIG_B64: ${{ secrets.ELIBRARY_OPENVPN_CONFIG_B64 }}
        run: |
          if [ -z "$ELIBRARY_OPENVPN_CONFIG_B64" ]; then
            echo "ELIBRARY_OPENVPN_CONFIG_B64 is not set; eLibrary/WoS will use the normal runner route or fallback data."
            exit 0
          fi
          umask 077
          printf '%s' "$ELIBRARY_OPENVPN_CONFIG_B64" | base64 --decode > home-openvpn.ovpn
          IP_BEFORE=$(curl -fsS --max-time 20 https://api.ipify.org || true)
          echo "Public IP before home VPN: ${IP_BEFORE:-not available}"
          sudo openvpn \
            --config home-openvpn.ovpn \
            --data-ciphers-fallback AES-128-CBC \
            --allow-compression yes \
            --daemon \
            --writepid openvpn.pid \
            --log openvpn.log
          for i in $(seq 1 60); do
            if ip link show tun0 >/dev/null 2>&1; then
              echo "Home VPN tun0 is up"
              break
            fi
            sleep 2
          done
          IP_AFTER=$(curl -fsS --max-time 30 https://api.ipify.org || true)
          echo "Public IP after home VPN: ${IP_AFTER:-not available}"
          if [ -n "$IP_AFTER" ] && [ "$IP_AFTER" != "$IP_BEFORE" ]; then
            echo "ELIBRARY_OPENVPN_ACTIVE=1" >> "$GITHUB_ENV"
            echo "WOS_HOME_OPENVPN_ACTIVE=1" >> "$GITHUB_ENV"
          else
            echo "OpenVPN did not change public IP; continuing with fallback logic."
            sudo tail -n 120 openvpn.log || true
          fi
      - name: Harvest eLibrary publication list through confirmed authors route
        env:
          ELIBRARY_AUTHOR_ID: "1012909"
          ELIBRARY_COOKIE: ${{ secrets.ELIBRARY_COOKIE }}
          ELIBRARY_ITEMS_OUT: data/processed/elibrary_publications.json
          ELIBRARY_BROWSER_REPORT: data/elibrary/browser_fetch_report.json
          ELIBRARY_BROWSER_WAIT_SEC: "130"
          ELIBRARY_BROWSER_HEADLESS: "false"
          ELIBRARY_USER_AGENT: "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/146.0.0.0 YaBrowser/26.4.0.0 Safari/537.36"
        run: |
          if [ -f scripts/harvest_elibrary_authors_route.py ]; then
            xvfb-run -a python scripts/harvest_elibrary_authors_route.py || true
          else
            echo "Production eLibrary authors-route harvester is missing; skipping browser route."
          fi
      - name: Harvest eLibrary author profile metrics
        env:
          ELIBRARY_AUTHOR_ID: "1012909"
          ELIBRARY_COOKIE: ${{ secrets.ELIBRARY_COOKIE }}
          ELIBRARY_PROFILE_URL: "https://www.elibrary.ru/author_profile.asp?id=1012909"
          ELIBRARY_PROFILE_OUT: data/elibrary/profile_metrics.json
          ELIBRARY_PROFILE_REPORT: data/elibrary/profile_metrics_fetch_report.json
          ELIBRARY_PROXY_URL: ${{ secrets.ELIBRARY_PROXY_URL }}
        run: |
          if [ -f scripts/harvest_elibrary_profile.py ] && [ -f scripts/parse_elibrary_author_profile.py ]; then
            python scripts/harvest_elibrary_profile.py || true
          else
            echo "eLibrary author profile harvester/parser is missing; using existing normalized metrics."
          fi
      - name: Harvest eLibrary publication list fallback
        env:
          ELIBRARY_AUTHOR_ID: "1012909"
          ELIBRARY_COOKIE: ${{ secrets.ELIBRARY_COOKIE }}
          ELIBRARY_ITEMS_URL: "https://elibrary.ru/author_items.asp?authorid=1012909&pubrole=100&show_refs=1&pubcat=risc"
          ELIBRARY_ITEMS_OUT: data/processed/elibrary_publications.json
          ELIBRARY_ITEMS_REPORT: data/elibrary/items_fetch_report.json
          ELIBRARY_PROXY_URL: ${{ secrets.ELIBRARY_PROXY_URL }}
        run: |
          if [ -f scripts/harvest_elibrary_items.py ] && [ -f scripts/parse_elibrary_author_items.py ]; then
            python scripts/harvest_elibrary_items.py || true
          else
            echo "eLibrary items harvester/parser is missing; using existing data/elibrary/publications.tsv."
          fi
      - name: Harvest Web of Science free-view author profile
        env:
          WOS_RESEARCHER_ID: "AAG-1530-2021"
          WOS_PROFILE_URL: "https://www.webofscience.com/wos/author/record/AAG-1530-2021"
          WOS_PROFILE_OUT: data/wos/profile_metrics.json
          WOS_HARVEST_REPORT: data/wos/harvest_report.json
          WOS_BROWSER_WAIT_SEC: "180"
          WOS_BROWSER_HEADLESS: "false"
          WOS_BROWSER_CHANNEL: "chrome"
          WOS_COOKIE: ${{ secrets.WOS_COOKIE }}
          WOS_STORAGE_STATE_B64: ${{ secrets.WOS_STORAGE_STATE_B64 }}
        run: |
          if [ -f scripts/harvest_wos_author_profile.py ]; then
            xvfb-run -a python scripts/harvest_wos_author_profile.py || true
          else
            echo "Web of Science free-view harvester is missing; using existing WOS snapshot/data."
          fi
      - name: Disconnect home OpenVPN route
        if: always()
        run: |
          if [ -f openvpn.pid ]; then
            sudo kill "$(cat openvpn.pid)" 2>/dev/null || true
            echo "Home OpenVPN route stopped."
          fi
          if [ -f openvpn.log ]; then
            echo "OpenVPN log tail:"
            sudo tail -n 120 openvpn.log || true
          fi
      - name: Harvest Scopus
        env:
          SCOPUS_API_KEY: ${{ secrets.SCOPUS_API_KEY }}
          SCOPUS_INST_TOKEN: ${{ secrets.SCOPUS_INST_TOKEN }}
          SCOPUS_AUTHOR_ID: ${{ inputs.scopus_author_id || '57220956828' }}
          SCOPUS_OUT_DIR: data/scopus
        run: |
          if [ -f scripts/harvest_scopus.py ] && [ -n "$SCOPUS_API_KEY" ]; then
            python scripts/harvest_scopus.py || true
          else
            echo "Scopus harvester or key is not configured; skipping."
          fi
      - name: Generate derived public data
        run: |
          if [ -f scripts/build_public_data.py ]; then
            python scripts/build_public_data.py || true
          else
            echo "No public data builder yet; skipping."
          fi
      - name: Translate publication titles to English
        run: |
          if [ -f scripts/translate_publication_titles.py ]; then
            python scripts/translate_publication_titles.py || true
          else
            echo "Publication-title translation script is missing; skipping."
          fi
      - name: Write refresh audit stamp
        run: |
          mkdir -p data/audit
          date -u +"%Y-%m-%dT%H:%M:%SZ" > data/audit/last_refresh_utc.txt
      - name: Commit data and mirrored assets
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add data/ assets/craftum/ || true
          if git diff --cached --quiet; then
            echo "No data changes."
          else
            git commit -m "chore: refresh portfolio data and assets"
            git push
          fi
